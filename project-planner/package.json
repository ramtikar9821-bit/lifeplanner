{
  "name": "lifeplanner",
  "version": "1.0.0",
  "description": "Life Planner App",
  "main": "index.html",
  "scripts": {
    "sync": "cap sync",
    "open": "cap open android",
    "build-android": "cap sync && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
